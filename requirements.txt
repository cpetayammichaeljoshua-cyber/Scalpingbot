# ═══════════════════════════════════════════════════════════════════════════════
# Unity Engine v11.1 — Production requirements.txt
# ALL versions pinned to currently-installed stable releases.
# Last verified: 2026-05-02
# ═══════════════════════════════════════════════════════════════════════════════

# ── Core async I/O ────────────────────────────────────────────────────────────
aiohttp==3.13.5
aiosqlite==0.22.1
aiofiles==25.1.0
aiodns==4.0.0
websockets==16.0
websocket-client==1.9.0

# ── Fast JSON serialization (~3-10× vs stdlib json) ──────────────────────────
orjson==3.11.8

# ── Redis async client for sub-millisecond state caching (optional) ──────────
redis==7.4.0

# ── Data processing ───────────────────────────────────────────────────────────
numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
ta==0.11.0

# ── Machine Learning / AI ─────────────────────────────────────────────────────
scikit-learn==1.8.0
openai==2.33.0
rank-bm25==0.2.2
torch==2.11.0+cpu

# ── Crypto exchange APIs ──────────────────────────────────────────────────────
ccxt==4.5.51
binance-connector==3.13.0

# ── Telegram bot ──────────────────────────────────────────────────────────────
python-telegram-bot[job-queue]==22.7

# ── HTTP / web utilities ──────────────────────────────────────────────────────
requests==2.33.1
beautifulsoup4==4.14.3
feedparser==6.0.12

# ── Scheduler ─────────────────────────────────────────────────────────────────
schedule==1.2.2

# ── Cryptography (ccxt / exchange auth) ────────────────────────────────────────
cryptography==47.0.0
pycryptodome==3.23.0

# ── High-performance event loop (Omega-Tier: 2-4× faster than CPython) ────────
uvloop==0.22.1

# ── System utilities ──────────────────────────────────────────────────────────
psutil==7.2.2

# ── Async rate-limiting ────────────────────────────────────────────────────────
asyncio-throttle==1.0.2

# ── PyTorch install note ───────────────────────────────────────────────────────
# torch must be installed separately from the PyTorch index:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
